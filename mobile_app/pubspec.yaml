name: yingxiao_ai_mobile
description: 映效AI工作站移动端，适配 Android 和 iOS 的图像、视频与模型库控制台。
publish_to: "none"
version: 0.1.8+9

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
